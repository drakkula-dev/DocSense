[project]
name = "docsense"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "James", email = "drakkula-dev@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.19.1",
    "fastapi[standard]>=0.141.1",
    "psycopg[binary]>=3.3.4",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
docsense = "docsense:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
